apiVersion: v1
kind: Service
metadata:
  name: yapi-svc
  labels: 
    name: yapi-svc
spec:
  type: NodePort
  ports:
  - port: 3200
    protocol: TCP
    targetPort: yapi
    nodePort: 3200
  selector:
    name: yapi-pod
  
# 我拿了较新的1.18.4版本，方便自定义修改，比较撤根据个lock文件判断初始化的