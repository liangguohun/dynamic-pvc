apiVersion: v1
kind: Service
metadata:
  name: redis-exp-svc
  labels: 
    name: redis-exp-svc
spec:
  type: NodePort
  ports:
  - port: 9090
    protocol: TCP
    targetPort: redis-exp
    name: redis-exp
    nodePort: 9090
  selector:
    name: redis-exp-pod