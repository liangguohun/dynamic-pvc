apiVersion: v1
kind: Service
metadata:
  name: codepush-svc
  labels: 
    name: codepush-svc
spec:
  type: NodePort
  ports:
  - name: codepushlogin
    port: 3200
    targetPort: codepushlogin
    nodePort: 3200
  selector:
    name: codepush-pod
